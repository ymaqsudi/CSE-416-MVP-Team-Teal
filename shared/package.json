{
    "name": "@tealcore/shared",
    "version": "0.0.1",
    "private": true,
    "type": "module",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "files": [
        "dist"
    ],
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "clean": "rm -rf dist",
        "test": "vitest run"
    },
    "devDependencies": {
        "@types/node": "^25.3.5",
        "typescript": "^5.0.0",
        "vitest": "^4.0.18"
    }
}
